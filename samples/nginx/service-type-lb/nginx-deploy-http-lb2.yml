apiVersion: v1
kind: ServiceAccount
metadata:
  name: nginx2
---
apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: nginx2
  name: nginx2-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx2
  strategy: 
    type: RollingUpdate
    rollingUpdate: 
      maxUnavailable: 1

  template:
    metadata:
      creationTimestamp: null
      labels:
        app: nginx2
      annotations:
        fluentbit.io/parser: nginx2
    spec:
#imagePullSecrets:
#      - name: harbor-registry-secret
      containers:
      - image: nginx
        #- image: infra-harbor.lab.pcfdemo.net/library/nginx2:latest
        name: nginx2-app
        imagePullPolicy: Always
        securityContext:
          allowPrivilegeEscalation: false
          runAsUser: 0
        resources: {}
      serviceAccount: nginx2
      serviceAccountName: nginx2
status: {}
---
apiVersion: v1
kind: Service
metadata:
  name: nginx2-app-service
spec:
  type: LoadBalancer
  #type: ClusterIP
  selector:
    app: nginx2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
